const hourly = require("./hourly");
const every4hour = require("./every4hour");
const every6hour = require("./every6hour");
const every12hour = require("./every12hour");
const daily = require("./daily");

module.exports = {
  hourly,
  every4hour,
  every6hour,
  every12hour,
  daily
};
